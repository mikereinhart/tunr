- @users.each do |user|
	= link_to "#{user['name'].capitalize}", user_path(user)
	%br/
	= link_to "Edit", edit_user_path(user)
	= link_to('delete', user_path(user), method: :delete, confirm: 'Are you sure you want to do this?') unless user == current_user
	%br/
	%br/
